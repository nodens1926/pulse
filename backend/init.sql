-- Создание расширения pgvector для эмбеддингов
CREATE EXTENSION IF NOT EXISTS vector;

-- Создание таблиц (если их нет, SQLAlchemy создаст автоматически)
-- Но оставляем для явного создания при инициализации
